{
  "name": "quote-generator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "serve",
    "build": "echo 'No build step required for static site'",
    "start": "serve"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
